import { useLoaderData } from "react-router-dom";

const ToyDetails = () => {
  const data = useLoaderData();
  console.log(data);
  return (
    <div>
      <h3>fkl</h3>
    </div>
  );
};

export default ToyDetails;
